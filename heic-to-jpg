#!/bin/sh
set -eu

. "$(dirname "$0")/lib/img-docker"

quality="${IMG_JPG_QUALITY:-85}"

for f in "$@"; do
  out="${f%.*}.jpg"
  img_run magick "$f" -auto-orient -quality "$quality" "$out"
  echo "$f -> $out"
done
